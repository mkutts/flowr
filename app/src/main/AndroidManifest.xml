<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@drawable/ic_splash_logo"
        android:label="@string/app_name"
        android:roundIcon="@drawable/ic_splash_logo"
        android:supportsRtl="true"
        android:theme="@style/Theme.Flowr">

        <!-- Disable/enable via buildType manifestPlaceholders -->
        <meta-data
            android:name="firebase_crashlytics_collection_enabled"
            android:value="${firebase_crashlytics_collection_enabled}" />

        <meta-data
            android:name="firebase_analytics_collection_deactivated"
            android:value="${firebase_analytics_collection_deactivated}" />

        <!-- Optional: keep automatic screen reporting off (less debug noise) -->
        <meta-data
            android:name="google_analytics_automatic_screen_reporting_enabled"
            android:value="false" />

        <!-- ✅ Launcher activity uses Theme.Flowr so API 31+ picks up splash attrs -->
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:theme="@style/Theme.Flowr">

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

        </activity>
    </application>

</manifest>
